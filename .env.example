# -----------------------------------------------------------------------------
# Macro Data Observatory
# Environment configuration template
# -----------------------------------------------------------------------------

# Copy this file to ".env" and replace the placeholder values.
# Never commit the generated ".env" file to version control.
# The ".env" file is ignored by Git through ".gitignore".

# SQLAlchemy PostgreSQL connection URL
DATABASE_URL=postgresql+psycopg2://username:password@localhost:5432/macro_db